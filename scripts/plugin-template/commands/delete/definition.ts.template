import type { SubcommandDefinition } from '@src/system/command-definition';

export const deleteDefinition = (
  prefix: string,
  alias: string,
): SubcommandDefinition => ({
  name: 'delete',
  summary: 'Delete a {{ALIAS}} row by id.',
  aliases: [],
  arguments: [
    {
      name: 'id',
      summary: 'Row id.',
      kind: 'integer',
      required: true,
      variadic: false,
    },
  ],
  options: [],
  examples: [`${prefix}${alias} delete 1`],
});
