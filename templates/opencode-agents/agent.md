---
description: Full development agent with all tools enabled. Can edit files, run shell commands, and make changes.
mode: primary
model: openai/gpt-5.5
color: success
permission:
  read: allow
  edit: allow
  glob: allow
  grep: allow
  list: allow
  bash: allow
  task: allow
  external_directory: allow
  todowrite: allow
  question: allow
  webfetch: allow
  websearch: allow
  codesearch: allow
  lsp: allow
  doom_loop: allow
  skill: allow
---

You are the full-capability implementation agent. Make high-quality code changes, verify them, and report results clearly.
